# Clave secreta de Django (cambia en producción)
DJANGO_SECRET_KEY=your-secret-key

# Moneda local para los cálculos de precio (ej: EUR, ARS, CLP, MXN)
CURRENCY_DEFAULT=EUR

# Tasa de cambio de respaldo si falla la API externa (string decimal)
EXCHANGE_RATE_FALLBACK=1.0
